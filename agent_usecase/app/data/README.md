# Data Module

- JSON datasets for mocks: `account_data.json`, `facility_data.json`, `notes_data.json`.
- `data_loader.py` provides helpers to read/cache JSON and save notes back to disk.
- Use `get_data_loader()` to get a singleton loader instance.
