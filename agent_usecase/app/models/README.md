# Models Module

- `request_models.py`: `AgentRequest` schema for chat inputs.
- `response_models.py`: `AgentResponse` and related typed models.
- `AgentResponse.model_json_schema()` is used to drive the agent response schema.
